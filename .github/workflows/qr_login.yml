name: TikTok QR Login Automation

on:
  workflow_dispatch:
    inputs:
      session_id:
        description: 'ID da sessão no Supabase (vem do Edge Function)'
        required: true
        type: string

jobs:
  run_qr_login:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependências Python
        run: |
          python -m pip install --upgrade pip
          pip install selenium supabase

      - name: Instalar Chrome + ChromeDriver
        uses: browser-actions/setup-chrome@latest
        id: chrome

      - name: Ver versão do Chrome
        run: ${{ steps.chrome.outputs.chrome-path }} --version

      - name: Executar script de login QR
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SESSION_ID: ${{ inputs.session_id }}
        run: |
          echo "Iniciando login QR para sessão: $SESSION_ID"
          python main.py "$SESSION_ID"

      - name: Workflow finalizado
        if: always()
        run: echo "Workflow concluído para sessão ${{ inputs.session_id }}"

